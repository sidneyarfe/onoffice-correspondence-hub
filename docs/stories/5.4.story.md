# Story 5.4 — Comércio: assinaturas + pedidos/avulsos + multi-produto na ficha

**Epic:** [005](./epic-005-modelo-dados-ideal-clientes.md) · **Fase:** Onda C
**Status:** Done (migração em prod; `tsc`/`lint`/`build` ✅)
**Dono:** @data-engineer + @dev + @ux-design-expert · Orquestração: @aiox-master (Orion)

## Story
Um cliente pode ter **N assinaturas + N avulsos** no mesmo cadastro, desacoplado do endereço fiscal.

## Acceptance Criteria
1. `cliente_planos` renomeada para `assinaturas` (VIEW de compat `security_invoker`, auto-updatable). ✅
2. `pedidos`/`pedido_itens` (avulsos: quantidade + unidade) com RLS admin + cliente. ✅
3. Aba **Plano & Financeiro** da ficha lista **assinaturas (recorrentes)** e **avulsos (venda única)**. ✅
4. Modal **"Vender produto"**: assinatura → insert em `assinaturas`; avulso → `pedido` + `pedido_item`. ✅
5. `types.ts` regenerado; gates verdes. ✅

## Dev Notes
- Rename seguro: guard idempotente + VIEW de compat com `security_invoker=true` (herda RLS; writes
  passam pela view). Consumidores legados de `cliente_planos` seguem funcionando.
- Hotfix junto: view `faturas` (5.2) recriada com `security_invoker` (RLS de pagamentos respeitada).
- Vencimento da nova assinatura: `calcularProximoVencimento(hoje, periodicidade)`.

## File List
- A `supabase/migrations/20260626170000_epic005_fix_faturas_security_invoker.sql` (aplicada)
- A `supabase/migrations/20260626180000_epic005_story54_assinaturas_pedidos.sql` (aplicada)
- M `src/integrations/supabase/types.ts`
- A `src/hooks/useClienteComercio.ts`
- A `src/components/admin/clientes/VenderProdutoModal.tsx`
- M `src/components/admin/clientes/ClienteFicha.tsx` (aba Financeiro multi-produto + modal)
- A `docs/stories/5.4.story.md`

## Change Log
- 2026-06-26 — migrações aplicadas pelo usuário via `! npx supabase db push`; @dev implementou UI multi-produto; Done.
