# Story 2.2 — Shells + dashboards (cliente/admin) em dark bento + fixes a11y

**Epic:** 002 — Design System 2.0 "Dark Premium"
**Status:** Done

> **PO Validation (Pax, 2026-06-12):** GO — 9/10. Reaproveita os 8 arquivos da story 1.4;
> incorpora TODOS os achados da avaliação UX (h1 duplicado, h2 antes de h1, Financeiro
> contraditório, interativo aninhado, touch targets). Depende da 2.1.

## Story

Como usuário, quero os dashboards (cliente e admin) no novo visual dark bento com glass,
para ter a experiência premium definida no épico.

## Acceptance Criteria

1. **Sidebars** (cliente/admin): superfície dark com glass sutil, item ativo lime com glow,
   labels de seção com contraste ≥4.5:1.
2. **Headers**: dark, único `h1` da tela movido para cá OU para o conteúdo (nunca ambos);
   headers usam `<p>`/`<span>` quando o h1 estiver no conteúdo.
3. **ClientOverview** em bento assimétrico: hero 2x2 glass sobre mesh lime com CTAs
   (Link estilizado, SEM `<button>` aninhado), KPIs 1x1, vencimento com glow quando ≤30d,
   aviso de desenvolvimento ABAIXO do hero (sem h2 antes de h1); quick action "Área
   financeira" marcada "Em breve" desabilitada (consistente com a sidebar).
4. **AdminOverview** dark bento: KPIs, health card na paleta da marca (sem verde Tailwind),
   atividades em rows dark.
5. Touch targets ≥40px nos botões de ação das listas.
6. Shells com fundo dark + mesh ambiente estático (sem animação — reduced-motion safe).
7. Gates: tsc + eslint (arquivos da story) + build verdes.

## Tasks

- [x] ClientSidebar + AdminSidebar dark glass
- [x] ClientHeader + AdminHeader dark (hierarquia de heading única)
- [x] ClientOverview bento (hero glass, aviso reposicionado, financeiro consistente)
- [x] AdminOverview bento dark
- [x] Shells (fundo + mesh)
- [x] Gates

## Dev Agent Record

### File List

- `src/components/client/ClientSidebar.tsx` — dark, logo light, ativo lime + glow,
  labels ≥4.5:1, footer glass
- `src/components/admin/AdminSidebar.tsx` — dark, badge Admin lime, logout vermelho dark
- `src/components/client/ClientHeader.tsx` — glass, h1→p (h1 único no hero), avatar glow
- `src/components/admin/AdminHeader.tsx` — glass, h1→p (h1 único na página), avatar lime
- `src/components/client/ClientOverview.tsx` — bento 12 col: hero glass 8-col com Links
  estilizados (sem button aninhado), tile vencimento com glow se ≤30d, aviso ABAIXO do
  hero, KPIs, recents dark, ações rápidas com Financeiro "Em breve" desabilitado
  (consistente com sidebar), botões de ação h-10 w-10 (touch target)
- `src/components/admin/AdminOverview.tsx` — sweep dark (skeletons, health na marca sem
  verde Tailwind, alerts dark, atividades, Receita em lime 10:1)
- `src/pages/client/ClientDashboard.tsx` / `admin/AdminDashboard.tsx` — `on-mesh`

### Change Log

- 2026-06-12: Implementação completa. Gates: tsc ✅, eslint --quiet ✅, build ✅ (21.71s);
  grep de classes claras nos 8 arquivos = 0.

## QA Results

**Verdict (Quinn, 2026-06-12): PASS ✅** — todos os achados da avaliação UX resolvidos
nesta superfície (h1 único, ordem de headings, financeiro consistente, sem interativo
aninhado, touch targets, contraste de labels). Smoke visual em browser segue pendente de
credenciais (mesma ressalva do épico).
