# Story 5.1 — Catálogo: tipo (assinatura/avulso) + avulsos

**Epic:** [005](./epic-005-modelo-dados-ideal-clientes.md) · **Fase:** Onda A
**Status:** Done (migração aplicada em prod; `tsc`/`lint`/`build` ✅)
**Dono:** @data-engineer + @dev · Orquestração: @aiox-master (Orion)

## Story
Distinguir no catálogo produtos de **assinatura** (recorrente) e **avulso** (venda única, ex.:
horas de sala), permitindo cadastrar ambos.

## Acceptance Criteria
1. `produtos.tipo` (`assinatura|avulso`, default assinatura) e `planos.unidade` existem no banco. ✅
2. `ProductFormModal` permite escolher o tipo. ✅
3. `PlanFormModal` mostra **Periodicidade** p/ assinatura e **Unidade** p/ avulso. ✅
4. A lista do catálogo (`AdminProducts`) exibe um badge com o tipo. ✅
5. `types.ts` regenerado; gates verdes. ✅

## File List
- A `supabase/migrations/20260626140000_epic005_story51_catalogo_tipo.sql` (aplicada em prod)
- M `src/integrations/supabase/types.ts` (regenerado)
- M `src/hooks/useProducts.ts` (`ProdutoTipo`, `Produto.tipo`, `Plano.unidade`, `normalizeProduto`)
- M `src/components/admin/ProductFormModal.tsx` (seletor de tipo)
- M `src/components/admin/PlanFormModal.tsx` (campo unidade condicional ao tipo)
- M `src/components/admin/AdminProducts.tsx` (badge de tipo)
- A `docs/stories/5.1.story.md`

## Change Log
- 2026-06-26 — migração aplicada pelo usuário via `! npx supabase db push`; @dev implementou forms; Done.
