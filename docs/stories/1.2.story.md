# Story 1.2 — Configuração Supabase por ambiente + Supabase CLI

**Epic:** 001 — Independência da Plataforma & Modernização
**Status:** Ready

> **PO Validation (Pax, 2026-06-12):** GO — 9/10. Fallback hardcoded garante zero
> regressão; blocker externo (login conta proprietária) corretamente isolado como ação
> do usuário com guia documentado (AC 4). Ressalva: `git rm --cached .env` muda estado
> do índice git — comunicar no relatório final.

## Story

Como equipe de desenvolvimento ON Office,
quero que o client Supabase leia configuração de variáveis de ambiente e que o projeto esteja
preparado para a Supabase CLI (link, types, deploy de functions),
para ter acesso total e versionado ao banco sem depender do pipeline do Lovable.

## Acceptance Criteria

1. `src/integrations/supabase/client.ts` e `src/lib/api.ts` leem
   `import.meta.env.VITE_SUPABASE_URL` / `VITE_SUPABASE_PUBLISHABLE_KEY` com fallback para os
   valores atuais (não quebra builds sem .env). Nenhuma key inline duplicada em
   `callEdgeFunction`.
2. `.env.example` contém as variáveis `VITE_SUPABASE_*` documentadas.
3. `package.json` ganha scripts: `typecheck`, `db:link`, `db:types`, `db:push`,
   `functions:deploy`.
4. `docs/guides/supabase-cli.md` criado com passo a passo: login com a conta proprietária,
   link, regeneração de types, migrations e deploy de Edge Functions — incluindo o erro de
   privilégio atual e como resolvê-lo.
5. `.env` deixa de ser rastreado pelo git (`git rm --cached .env`) — arquivo local
   preservado; `.gitignore` já cobre.
6. Lint + typecheck + build verdes.

## Tasks

- [ ] Refatorar `client.ts` para env com fallback
- [ ] Refatorar `lib/api.ts` (URL + key de env; remover key inline do header)
- [ ] Atualizar `.env.example` com bloco VITE_SUPABASE_*
- [ ] Adicionar scripts npm (typecheck, db:link, db:types, db:push, functions:deploy)
- [ ] Criar `docs/guides/supabase-cli.md`
- [ ] `git rm --cached .env`
- [ ] Rodar gates

## Dev Notes

- **Blocker externo:** `supabase link --project-ref ifpqrugbpzqpapoaameo` retorna erro de
  privilégio com a conta CLI atual. Ação do usuário: `supabase login` com a conta
  proprietária do projeto (a usada no Lovable) e rodar `npm run db:link`.
- Anon key é pública por design (RLS protege os dados) — fallback hardcoded é aceitável e
  mantém compatibilidade.
- `supabase/config.toml` já tem `project_id` correto.

## Dev Agent Record

### File List

### Change Log

## QA Results
