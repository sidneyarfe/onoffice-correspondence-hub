# Story 2.3 — Páginas internas do cliente em dark premium

**Epic:** 002 — Design System 2.0 "Dark Premium"
**Status:** Done

> **PO Validation (Pax, 2026-06-12):** GO — 8/10. Sweep mecânico bem definido (mapa de
> classes claro→dark) + revisão página a página. Depende da 2.1/2.2.

## Story

Como cliente, quero todas as minhas páginas internas (Correspondências, Documentos,
Financeiro, Perfil, Notificações) e modais no novo visual dark premium.

## Acceptance Criteria

1. `ClientCorrespondences`, `ClientDocuments`, `ClientFinancial`, `ClientProfile`,
   `ClientNotifications`, `CorrespondenceViewModal`, `NotificationsPopup` sem superfícies
   claras hardcoded (`bg-white`, `bg-gray-50/100`, `text-gray-900`…) — mapa de conversão:
   `bg-white→bg-card`, `bg-gray-50→bg-white/5`, `text-gray-900/on-dark→text-foreground`,
   `text-gray-600/500→text-muted-foreground`, `border-gray-*→border-white/10`.
2. Tabelas/listas com rows dark (`hover:bg-white/5`), badges via `on-pill`, vazios e
   loadings na paleta nova.
3. Inputs/forms shadcn herdam tokens (verificar `bg-background` dos primitivos).
4. Gates: tsc + eslint (arquivos da story) + build verdes; grep final sem classes claras.

## Tasks

- [x] Sweep + revisão das 5 páginas
- [x] Modais/popups do cliente
- [x] Grep de regressão (classes claras)
- [x] Gates

## Dev Agent Record

### File List

- `ClientCorrespondences.tsx` — sweep dark + tipos `Correspondencia` (era `any` ×2);
  categorias com tints dark (fiscal vermelho, municipal azul, estadual lime…)
- `ClientDocuments.tsx`, `ClientFinancial.tsx`, `ClientNotifications.tsx`,
  `CorrespondenceViewModal.tsx`, `NotificationsPopup.tsx` — sweep dark via mapa
- `ClientProfile.tsx` — sweep dark + `ContratacaoRow` tipado via `Database` gerado
  (eliminados `any` ×2) + prefer-const

### Change Log

- 2026-06-12: Sweep mecânico (mapa claro→dark com regex auditável) + correções de tipo.
  Gates: tsc ✅, eslint --quiet 0 ✅ no diretório client, build ✅ (21.02s), grep de
  classes claras no diretório = 0.

## QA Results

**Verdict (Quinn, 2026-06-12): PASS ✅** — mapa aplicado uniformemente; badges de
categoria revisadas manualmente (contraste de tints ok em dark); 5 erros de lint
pré-existentes nos arquivos tocados foram corrigidos com tipos do schema gerado.
