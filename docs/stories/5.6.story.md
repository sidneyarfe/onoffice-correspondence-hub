# Story 5.6 — Identidade: clientes (rename) + enderecos_fiscais

**Epic:** [005](./epic-005-modelo-dados-ideal-clientes.md) · **Fase:** Onda C (finale)
**Status:** Done (migração aplicada em prod; `tsc`/`build` ✅; lint dos arquivos do épico limpo)
**Dono:** @data-engineer + @dev · Orquestração: @aiox-master (Orion)

## Story
Renomear `contratacoes_clientes → clientes` (refletir a lógica de identidade) e desacoplar o
endereço fiscal (recurso atribuído, não o endereço do cliente), sem quebrar nada.

## Acceptance Criteria
1. `contratacoes_clientes` renomeada para `clientes` + **VIEW de compat** (`security_invoker`,
   auto-updatable) → 25 usos diretos + 7 edge functions seguem funcionando sem mudança. ✅
2. `enderecos_fiscais` (pool atribuível) + `assinaturas.endereco_fiscal_id` (FK 1:1) + RLS. ✅
3. Policies de `pedidos`/`contratos` re-apontadas para `clientes`. ✅
4. `types.ts` regenerado (`clientes` tabela, `contratacoes_clientes` view); gates verdes. ✅

## Dev Notes
- **Análise de impacto (read-only) antes do push:** 20 arquivos frontend + 7 edge fns + RLS/FKs;
  **ZERO embeds PostgREST** → a view de compat cobre tudo. FKs/RLS/triggers seguem o rename por OID.
- Migração gradual dos consumidores `contratacoes_clientes → clientes` é follow-up (sem pressa; a
  view mantém compat). Endereço do cliente continua em `clientes.endereco`; o fiscal vai p/ o pool.

## File List
- A `supabase/migrations/20260627120000_epic005_story56_clientes_enderecos_fiscais.sql` (aplicada)
- M `src/integrations/supabase/types.ts`
- A `docs/stories/5.6.story.md`

## Change Log
- 2026-06-27 — análise de impacto + plano apresentados; migração aplicada pelo usuário via push; Done.
  Epic 005 concluído (5.0–5.6).
