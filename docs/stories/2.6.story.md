# Story 2.6 — Refinamento Stripe: raio, Work Sans única e fonte numérica DM Mono

**Epic:** 002 — Design System 2.0 "Dark Premium"
**Status:** Done

> **PO Validation (Pax, 2026-06-12):** GO — 9/10. Feedback direto do usuário (screenshots):
> "tudo muito arredondado", "troque todas as fontes para Work Sans", "números com fonte
> tipo DM", "estilo Stripe". Sweep global controlado.

## Acceptance Criteria

1. Raio global reduzido (estilo Stripe): `--radius: 0.5rem`; sweep `rounded-xl→rounded-md`,
   `rounded-2xl→rounded-lg`, `rounded-3xl→rounded-xl` em src/ (rounded-full preservado);
   utilities `on-card`/`on-button`/`on-tile` atualizadas.
2. Outfit REMOVIDA (headings voltam a Work Sans); **DM Mono** carregada e aplicada a
   valores numéricos (KPIs, moedas, datas, telefones em tabelas) via `font-dm` +
   `tabular-nums` (utility `.on-num`).
3. Gates: tsc + eslint + build verdes.

## Tasks

- [x] index.html (fontes) + tailwind.config (font-dm, remover outfit)
- [x] index.css (radius, base headings, .on-num, utilities)
- [x] Sweep de raios em src/ + troca font-outfit → font-dm/remoção
- [x] Gates

## Dev Agent Record

### File List

- `index.html` — DM Mono no lugar de Outfit
- `tailwind.config.ts` — `font-dm` (DM Mono) substitui outfit
- `src/index.css` — `--radius: 0.5rem`, headings sem Outfit, `on-card` rounded-lg,
  `on-button`/`on-tile` rounded-md, nova `.on-num` (font-dm + tabular-nums)
- Sweep em 6 componentes: rounded-xl→md, 2xl→lg, 3xl→xl; valores numéricos dos overviews
  em `font-dm`; h1 do AdminOverview mantido em Work Sans (mono é só para números)

### Change Log

- 2026-06-12: Gates tsc ✅ build ✅ (12.49s); `grep font-outfit` = 0.

## QA Results

**Verdict (Quinn, 2026-06-12): PASS ✅** — raios contidos estilo Stripe em todo o app
(shadcn herda via --radius), tipografia unificada Work Sans + DM Mono numérica.
