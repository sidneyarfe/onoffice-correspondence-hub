# Story 5.5 — Contratos como entidade + enviar contrato ZapSign

**Epic:** [005](./epic-005-modelo-dados-ideal-clientes.md) · **Fase:** Onda C
**Status:** Code-complete — ⚠️ pendente push da migração + deploy da edge function + secret ZapSign
**Dono:** @dev + @data-engineer · Orquestração: @aiox-master (Orion)

## Story
Enviar o contrato automaticamente ao cliente (modal estilo Cobrança): gera o documento no ZapSign
a partir do template do plano, autopreenche com os dados do cliente e dispara o e-mail de assinatura.

## Acceptance Criteria
1. Tabela `contratos` (entidade própria; extrai `zapsign_*`) com RLS admin + cliente. ✅ (migração)
2. Edge function `enviar-contrato`: `POST .../models/create-doc/` com `template_id` do plano
   (PF/PJ), `data` (dataVars), `send_automatic_email`; grava `contratos` + reflete em
   `contratacoes_clientes`. ✅
3. Modal **"Enviar contrato"** na ficha (gated p/ `iniciado`/`contrato_enviado`), com aviso se o
   plano não tiver template. ✅
4. Gates frontend verdes. ✅

## Gates pendentes (do usuário / @devops)
- `! npx supabase db push` (aplica `20260626190000_…contratos.sql`)
- `! npx supabase functions deploy enviar-contrato`
- Secret **`ZAPSIGN_API_TOKEN`** configurado no projeto (a function aborta com 500 se faltar).

## File List
- A `supabase/migrations/20260626190000_epic005_story55_contratos.sql`
- A `supabase/functions/enviar-contrato/index.ts`
- M `supabase/config.toml` (functions.enviar-contrato, verify_jwt=false)
- A `src/components/admin/clientes/EnviarContratoModal.tsx`
- M `src/components/admin/clientes/ClienteFicha.tsx` (botão "Enviar contrato" + modal)
- A `docs/stories/5.5.story.md`

## Dev Notes
- Templates por plano em `planos.zapsign_template_id_pf/_pj`; dataVars de `docs/architecture/n8n-sistema-on-reference.md`.
- O `zapsign-webhook` existente marca `CONTRATO_ASSINADO` quando o cliente assina (external_id = contratacao_id).

## Change Log
- 2026-06-27 — implementado após reconexão do drive D: (queda durante a 5.5). Code-complete; aguardando deploy.
