# Story 5.2 — Cobrança como verdade: faturas + "vencido"

**Epic:** [005](./epic-005-modelo-dados-ideal-clientes.md) · **Fase:** Onda B
**Status:** Done (migração aplicada em prod; `tsc` ✅) — ⚠️ agendamento automático pendente de `pg_cron`
**Dono:** @data-engineer + @dev · Orquestração: @aiox-master (Orion)

## Story
Estabelecer a camada de cobrança ideal (`faturas`) e o "vencido" derivado + persistível, sem
quebrar `pagamentos` nem os valores vivos.

## Acceptance Criteria
1. View `public.faturas` sobre `pagamentos` com `status` derivado (`fatura_status_efetivo`). ✅
2. Função `marcar_faturas_vencidas()` (idempotente) que carimba `vencida_em`. ✅
3. Colunas aditivas `valor_centavos` (backfill) + `vencida_em` em `pagamentos`. ✅
4. `types.ts` regenerado (view `faturas` tipada). ✅
5. Agendamento diário. 🟡 **`pg_cron` ausente no projeto** — a migração emitiu NOTICE; agendar
   via toggle do `pg_cron` (Dashboard → Database → Extensions) ou cron externo apontando p/ a fn.

## Dev Notes
- ADITIVA: NÃO renomeia/destrói `pagamentos` (muitos consumidores). `faturas` é VIEW (camada ideal);
  `valor` (reais) preservado, `valor_centavos` ao lado.
- "Vencido" é estado da FATURA (modelo past_due). O dashboard (5.3) deriva na leitura; o job persiste.
- **Follow-up:** habilitar `pg_cron` e re-rodar o bloco de `cron.schedule`, OU criar edge fn cron.

## File List
- A `supabase/migrations/20260626160000_epic005_story52_faturas_vencido.sql` (aplicada em prod)
- M `src/integrations/supabase/types.ts` (view faturas + colunas)
- A `docs/stories/5.2.story.md`

## Change Log
- 2026-06-26 — migração aplicada pelo usuário via `! npx supabase db push`; pg_cron ausente (agendamento pendente).
